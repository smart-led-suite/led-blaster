#ifndef _desktop_config_h
#define _desktop_config_h
/*
in order to simplify tests, DESKTOP mode was introduced which is
removing all gpio io so it can be used on any pc for developing.
this variable moved to a seperate file so it can be left as is
and versions can be exchanged between DESKTOP and Pi without changes.
*/
#define DESKTOP


#endif //desktop config
